import { ModulePlaceholder } from '@/components/module-placeholder';

export default function PlanScreen() {
  return (
    <ModulePlaceholder
      title="Plan"
      subtitle="See and tune one person's day against their targets, and chat to adjust."
      status="Coming in M2"
    />
  );
}
