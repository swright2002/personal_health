import { ModulePlaceholder } from '@/components/module-placeholder';

export default function RecipesScreen() {
  return (
    <ModulePlaceholder
      title="Recipes"
      subtitle="Search, filter, and add recipes to the plan — plus a recipe editor and AI-assisted import."
      status="Coming in M2"
    />
  );
}
