import { ModulePlaceholder } from '@/components/module-placeholder';

export default function ShoppingScreen() {
  return (
    <ModulePlaceholder
      title="Shopping"
      subtitle="One combined grocery run for the household, grouped by store, synced between phones."
      status="Coming in M2"
    />
  );
}
